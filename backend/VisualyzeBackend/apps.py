from django.apps import AppConfig

"""
A Django "app" is a self-contained module of code that handles a specific function or feature within 
a larger Django project. They are designed to be modular and reusable across different projects, adhering 
to the "Don't Repeat Yourself" (DRY) principle. 
"""

class VisualyzebackendConfig(AppConfig):
    name = 'VisualyzeBackend'
